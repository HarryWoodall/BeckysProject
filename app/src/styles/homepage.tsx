import * as React from "react";
import { HomepageStyles } from "../common/types";
import { StyleSheet } from "react-native";

const Homepage = StyleSheet.create<HomepageStyles>({
  searchBar: {},
  addNewButton: {
    marginTop: 20,
  },
  icon: {},
});

export default Homepage;
